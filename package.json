{
  "name": "essentia-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": "18.x" },
  "scripts": {
    "build": "echo 'Build not required for API-only project'",
    "start": "node api/hello.js"
  },
  "dependencies": {
    "@vercel/node": "3.0.0"
  }
}

