services:
  - type: web
    name: flow-ecosystem
    env: node
    buildCommand: npm run build
    startCommand: npm run start
    plan: free
    region: oregon
    envVars:
      - key: NODE_ENV
        value: production